//- Copyright (C) 2019 - 2020 Ryan Bester
extends ../layout

block admin-content
    if hasPermission
        if error
            h2.error= error
        if success
            h2.success= success
        form.myaccount-form(method='POST' action='')
            .form-row
                label(for='admin-user-name-form-username') Username
                input#admin-user-name-form-username(
                    class=(usernameInvalid ? 'is-invalid' : undefined)
                    type='text'
                    name='username'
                    placeholder='Username'
                    value= username
                )
                .invalid-feedback This username has been taken
            br
            input(type='hidden' name='nonce' value=formNonce)
            input(
                type='submit'
                value='Save'
            )
    else
        p You do not have permission to change a user's username.