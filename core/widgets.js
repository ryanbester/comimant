/*
Copyright (C) 2019 Bester Intranet
*/

const mysql = require('mysql');

const db = require('../db/db');

module.exports.Widget = class Widget {

}

module.exports.WidgetManager = class WidgetManager {
    // constructor(user_id)

    // getWidgets(user_id)
    // addWidget(widget, user_id)
    // getSkeleton(user_id)
    // deleteWidget(user_id)
    // updateWidget(widget, user_id)
    // getWidget(user_id, widget_id)
}